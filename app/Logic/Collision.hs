module Collision (collide) where
import State
{-
a general collide function, which should be wrap all the complex math of checking to see if any object collide with each other
-}
collide :: State -> State 
State
